import * as React from 'react';
const className = require('./side-bar.css');

export const SideBarComponent = () => (
  <div id='mySidenav' className={className.sidenav}>
    <span>Basic Sidebar, first step</span>
  </div>
);
